// Define the currentUser variable and assign it a string value
const currentUser = "Grace Hopper";

// Define the welcomeMessage variable and use string interpolation to incorporate currentUser
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Define the excitedWelcomeMessage variable by converting welcomeMessage to uppercase
const excitedWelcomeMessage = welcomeMessage.toUpperCase();

// Define the shortGreeting variable using the first letter of currentUser
const shortGreeting = `Welcome, ${currentUser[0]}!`;